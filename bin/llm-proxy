#!/usr/bin/env ruby
# frozen_string_literal: true

begin
  require "bundler/setup"
rescue LoadError
  # Run without bundler (system gem install)
end
require_relative "../lib/llm_proxy"
LLMProxy::CLI.run!
